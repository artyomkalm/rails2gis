require 'spec_helper'

describe Rails2gis do
  it 'has a version number' do
    expect(Rails2gis::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
